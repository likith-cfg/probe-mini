description = "Check whether a GCP monitoring change or deployment actually succeeded or failed."
prompt = "Use the probe-verify skill to check whether the deployment/change described in $ARGUMENTS succeeded. Run the layered checks (GCP API existence, Cloud Audit Logs root-cause, SRE Advisor if a CHG number is available) and report findings plainly. If a real error is found, propose a fix."
